# Trading Service Configuration
# Copy to .env and fill in values

# ── Encryption ──────────────────────────────────────────
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
TS_ENCRYPTION_KEY=

# ── Database ────────────────────────────────────────────
# Default: sqlite:///data/trading.db
# TS_DATABASE_URL=sqlite:///data/trading.db

# ── Logging ─────────────────────────────────────────────
# TS_LOG_LEVEL=INFO

# ── CORS ────────────────────────────────────────────────
# Comma-separated origins (not needed when serving frontend from FastAPI)
# TS_CORS_ORIGINS=["http://localhost:5173"]

# ── Authentication ──────────────────────────────────────
# Change this to a random secret in production!
TS_JWT_SECRET=change-me-in-production
# TS_JWT_EXPIRE_MINUTES=1440

# ── Telegram Bot (optional) ─────────────────────────────
# Get token from @BotFather
# TS_TELEGRAM_BOT_TOKEN=
# Comma-separated Telegram user IDs allowed to control the bot
# TS_TELEGRAM_CHAT_IDS=[123456789]

# ── Docker / Caddy ──────────────────────────────────────
# Domain for automatic HTTPS (Caddy)
# DOMAIN=trading.example.com
